
= jetbrains

[source,shell]
----

# 下载
powershell
Invoke-WebRequest -Uri 'https://ckey.run/ja-netfilter' -OutFile 'ja-netfilter.zip'

# 下载完，需要转成 gbk + lf 再运行
Invoke-WebRequest -Uri 'https://ckey.run' -OutFile 'ckey.run.ps1'

Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\ckey.run.ps1

powershell -Command "Invoke-WebRequest -Uri 'https://ckey.run/ja-netfilter' -OutFile 'ja-netfilter.zip'"
powershell -Command "Invoke-WebRequest -Uri 'https://ckey.run' -OutFile 'ckey.run.ps1'"
powershell -ExecutionPolicy Bypass -File ".\ckey.run.ps1"

----

[source,shell]
----

irm ckey.run | iex

irm ckey.run/debug | iex

irm ckey.run/uninstall | iex

----

https://ckey.run/ja-netfilter
